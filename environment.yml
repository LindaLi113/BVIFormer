name: dehaze-bcf
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - numpy>=1.23
      - pillow>=9.5
      - tqdm>=4.66
      - pyyaml>=6.0
      - opencv-python>=4.8
      - scikit-image>=0.22
      # For CPU-only PyTorch wheels:
      - torch==2.3.1
      - torchvision==0.18.1
