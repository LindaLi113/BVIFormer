# Minimal runtime deps for inference/evaluation only
# Training deps are intentionally excluded during review.
numpy>=1.23
pillow>=9.5
tqdm>=4.66
pyyaml>=6.0
opencv-python>=4.8
scikit-image>=0.22
# Install torch/torchvision matching your CUDA/CPU:
# Example CPU only:
torch==2.3.1
torchvision==0.18.1
